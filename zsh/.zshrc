# .zshrc — reference config
# Copy to ~/.zshrc or source from there

# larasearch
export PATH="$HOME/.larasearch:$PATH"

# Shared
DOTFILES="$HOME/Desktop/Projects/dotfiles"
source "$DOTFILES/shared/env.sh"
source "$DOTFILES/shared/aliases.sh"
source "$DOTFILES/shared/functions.sh"
